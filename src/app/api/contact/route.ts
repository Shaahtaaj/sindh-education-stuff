import { NextRequest, NextResponse } from "next/server"; import { contactSchema } from "@/lib/validations"; import { rateLimit } from "@/lib/rate-limit"; import { sanitizeText } from "@/lib/utils";
export async function POST(req:NextRequest){const ip=req.headers.get("x-forwarded-for")?.split(",")[0]??"unknown";if(!rateLimit(`contact:${ip}`,5,10*60_000).allowed)return NextResponse.json({error:"Too many requests."},{status:429});const parsed=contactSchema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid form data."},{status:400});const safe=Object.fromEntries(Object.entries(parsed.data).map(([k,v])=>[k,sanitizeText(v)]));console.info("Contact request received",{...safe,message:"[redacted]"});return NextResponse.json({ok:true},{status:201})}
