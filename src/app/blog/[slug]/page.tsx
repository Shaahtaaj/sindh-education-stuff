import type { Metadata } from "next"; import { notFound } from "next/navigation"; import { Breadcrumbs } from "@/components/Breadcrumbs"; import { posts } from "@/data/content"; import { formatDate } from "@/lib/utils"; import { SITE_URL } from "@/lib/constants";
type Props={params:Promise<{slug:string}>};
export function generateStaticParams(){return posts.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const {slug}=await params;const p=posts.find(x=>x.slug===slug);return p?{title:p.title,description:p.excerpt,alternates:{canonical:`/blog/${slug}`},openGraph:{type:"article",title:p.title,description:p.excerpt}}:{}}
export default async function Page({params}:Props){const {slug}=await params;const p=posts.find(x=>x.slug===slug);if(!p)notFound();const schema={"@context":"https://schema.org","@type":"Article",headline:p.title,datePublished:p.date,dateModified:p.date,author:{"@type":"Organization","name":p.author},publisher:{"@type":"Organization","name":"Sindh Education Stuff"},mainEntityOfPage:`${SITE_URL}/blog/${p.slug}`};return <article><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema).replace(/</g,"\\u003c")}}/><header className="bg-[#f4f7f9] py-14 md:py-20"><div className="container-site max-w-4xl"><Breadcrumbs items={[{label:"Blog",href:"/blog"},{label:p.title}]}/><p className="mt-10 text-xs font-bold uppercase tracking-[.1em] text-[#147a4b]">{p.category}</p><h1 className="mt-4 text-[clamp(2.5rem,5vw,4.8rem)] font-extrabold leading-[1] tracking-[-.055em] text-[#0b1f3a]">{p.title}</h1><p className="mt-6 text-lg leading-8 text-[#607086]">{p.excerpt}</p><p className="mt-7 text-sm font-semibold text-[#405169]">By {p.author} · Updated {formatDate(p.date)} · {p.readTime}</p></div></header><div className="container-site max-w-3xl py-16"><div className="rich-content"><p>Strong academic work starts before the first paragraph. Read the task carefully, identify the action words and decide what evidence each part needs.</p><h2>Start with the question</h2><p>Break a broad prompt into smaller decisions. Note the course concepts that are directly relevant and avoid adding material simply to increase length.</p><h2>Build a simple structure</h2><p>Use a short introduction, clear headings and connected paragraphs. Each paragraph should make one point, support it and explain why it matters to the question.</p><h2>Keep the work original</h2><p>Sources should help you understand and support ideas, not replace your own reasoning. Paraphrase carefully, cite sources consistently and review current university guidance before submission.</p></div></div></article>}
