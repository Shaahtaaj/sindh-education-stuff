import { BlogCard } from "@/components/BlogCard"; import { PageHero } from "@/components/PageHero"; import { getPublicBlogs } from "@/lib/public-content";
export const metadata={title:"Education Blog",description:"Practical articles about AIOU assignments, research and teaching practice."};
export const dynamic="force-dynamic";
export default async function Page(){const posts=await getPublicBlogs();return <><PageHero title="Study guidance blog" description="Practical articles for students who want to plan better, write clearly and make responsible academic decisions."/><section className="section"><div className="container-site max-w-4xl">{posts.length?posts.map(post=><BlogCard key={post.slug} post={{...post,date:post.publishDate}}/>):<div className="py-16 text-center"><p className="text-lg font-bold text-[#0b1f3a]">No published articles yet</p><p className="mt-2 text-sm text-[#607086]">Published articles from Blog Studio will appear here.</p></div>}</div></section></>}
