import { PageHero } from "@/components/PageHero"; import { getPublicGeneric } from "@/lib/public-content"; import { formatDate } from "@/lib/utils";
export const metadata={title:"Assignment Dates",description:"A convenient reference for AIOU assignment submission dates. Verify all dates officially."};
export const dynamic="force-dynamic";
export default async function Page(){const rows=await getPublicGeneric("assignment-dates");return <><PageHero title="Assignment dates" description="Use this schedule as a planning aid and always confirm current dates through official AIOU channels before submission."/><section className="section"><div className="container-site overflow-x-auto"><table className="w-full min-w-[680px] border-collapse text-left"><thead><tr className="border-b-2 border-[#0b1f3a] text-xs uppercase tracking-[.08em] text-[#607086]">{["Program","Semester","Assignment","Due date","Status"].map(x=><th key={x} className="px-4 py-4">{x}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={row.id} className="border-b border-[#dfe6ec]"><td className="px-4 py-5 font-bold text-[#0b1f3a]">{row.values[0]}</td><td className="px-4 py-5 text-[#607086]">{row.values[1]}</td><td className="px-4 py-5">{row.values[2]}</td><td className="px-4 py-5 font-semibold">{formatDate(row.values[3])}</td><td className="px-4 py-5 text-sm font-bold text-[#147a4b]">{row.values[4]}</td></tr>)}</tbody></table>{!rows.length?<p className="py-12 text-center text-[#607086]">No published assignment dates yet.</p>:null}<p className="mt-6 text-sm leading-6 text-[#607086]">Important: This independent schedule may not reflect official changes. Confirm dates on official AIOU channels.</p></div></section></>}
