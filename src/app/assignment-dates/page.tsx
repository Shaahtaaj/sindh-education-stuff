import { PageHero } from "@/components/PageHero"; import { assignmentDates } from "@/data/content"; import { formatDate } from "@/lib/utils";
export const metadata={title:"Assignment Dates",description:"A convenient reference for AIOU assignment submission dates. Verify all dates officially."};
export default function Page(){return <><PageHero title="Assignment dates" description="Use this schedule as a planning aid and always confirm current dates through official AIOU channels before submission."/><section className="section"><div className="container-site overflow-x-auto"><table className="w-full min-w-[680px] border-collapse text-left"><thead><tr className="border-b-2 border-[#0b1f3a] text-xs uppercase tracking-[.08em] text-[#607086]">{["Program","Semester","Assignment","Due date","Status"].map(x=><th key={x} className="px-4 py-4">{x}</th>)}</tr></thead><tbody>{assignmentDates.map(x=><tr key={`${x.program}-${x.assignment}`} className="border-b border-[#dfe6ec]"><td className="px-4 py-5 font-bold text-[#0b1f3a]">{x.program}</td><td className="px-4 py-5 text-[#607086]">{x.semester}</td><td className="px-4 py-5">{x.assignment}</td><td className="px-4 py-5 font-semibold">{formatDate(x.dueDate)}</td><td className="px-4 py-5 text-sm font-bold text-[#147a4b]">{x.status}</td></tr>)}</tbody></table><p className="mt-6 text-sm leading-6 text-[#607086]">Important: This independent schedule may not reflect official changes. Confirm dates on official AIOU channels.</p></div></section></>}
