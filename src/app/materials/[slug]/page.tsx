import type { Metadata } from "next"; import Link from "next/link"; import { notFound } from "next/navigation"; import { Download, LockKeyhole } from "lucide-react"; import { Breadcrumbs } from "@/components/Breadcrumbs"; import { MaterialCard } from "@/components/MaterialCard"; import { materials } from "@/data/content"; import { formatDate } from "@/lib/utils";
type Props={params:Promise<{slug:string}>}; export function generateStaticParams(){return materials.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:Props):Promise<Metadata>{const {slug}=await params;const x=materials.find(m=>m.slug===slug);return x?{title:x.title,description:x.excerpt,alternates:{canonical:`/materials/${slug}`}}:{}}
export default async function Page({params}:Props){const {slug}=await params;const x=materials.find(m=>m.slug===slug);if(!x)notFound();const related=materials.filter(m=>m.slug!==x.slug&&m.category===x.category).slice(0,3);return <><header className="bg-[#f4f7f9] py-14 md:py-20"><div className="container-site"><Breadcrumbs items={[{label:"Resources",href:"/resources"},{label:x.title}]}/><div className="mt-8 grid items-end gap-10 lg:grid-cols-[1fr_300px]"><div><p className="text-xs font-bold uppercase tracking-[.1em] text-[#147a4b]">{x.category} · {x.program}</p><h1 className="mt-4 max-w-4xl text-[clamp(2.5rem,5vw,4.6rem)] font-extrabold leading-[1] tracking-[-.055em] text-[#0b1f3a]">{x.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-[#607086]">{x.excerpt}</p></div><div className="rounded-2xl bg-white p-6 shadow-sm"><p className="text-sm text-[#607086]">Course</p><p className="mt-1 text-xl font-extrabold text-[#0b1f3a]">{x.courseCode}</p><p className="mt-4 text-sm text-[#607086]">Updated {formatDate(x.updatedAt)}</p>{x.isFree?<button className="mt-6 flex w-full items-center justify-center gap-2 rounded-xl bg-[#147a4b] px-5 py-3.5 font-bold text-white"><Download size={18}/>Download free guide</button>:<Link href={`/order-help?material=${x.slug}`} className="mt-6 flex items-center justify-center gap-2 rounded-xl bg-[#147a4b] px-5 py-3.5 font-bold text-white"><LockKeyhole size={18}/>Order Help · PKR {x.price}</Link>}</div></div></div></header><section className="section"><div className="container-site max-w-4xl"><div className="rich-content"><h2>What this guide covers</h2><p>This resource explains the expected structure, gives practical planning prompts and helps you check formatting before submission.</p><ul><li>Understand the purpose and requirements of the task.</li><li>Plan headings and evidence before drafting.</li><li>Use clear formatting and consistent references.</li><li>Review your own original work against a final checklist.</li></ul><h2>Responsible use</h2><p>Use this material for learning, reference and formatting support. Adapt the guidance to your course instructions and prepare your own original submission.</p></div></div></section>{related.length>0&&<section className="section bg-[#f4f7f9]"><div className="container-site"><h2 className="section-title">Related material</h2><div className="mt-10 grid gap-8 md:grid-cols-3">{related.map(m=><MaterialCard key={m.slug} item={m}/>)}</div></div></section>}</>}
