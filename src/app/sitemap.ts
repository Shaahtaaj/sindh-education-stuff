import type { MetadataRoute } from "next"; import { materials, posts } from "@/data/content"; import { SITE_URL } from "@/lib/constants";
export default function sitemap():MetadataRoute.Sitemap{const staticRoutes=["","/about","/contact","/privacy-policy","/terms","/disclaimer","/dmca","/assignments","/research-8613","/lesson-plans","/field-notes","/guess-papers","/assignment-dates","/tutor-guide","/blog","/resources","/faqs"];return [...staticRoutes.map(route=>({url:`${SITE_URL}${route}`,lastModified:new Date(),changeFrequency:route===""?"daily" as const:"weekly" as const,priority:route===""?1:.7})),...materials.map(x=>({url:`${SITE_URL}/materials/${x.slug}`,lastModified:new Date(x.updatedAt),changeFrequency:"monthly" as const,priority:.6})),...posts.map(x=>({url:`${SITE_URL}/blog/${x.slug}`,lastModified:new Date(x.date),changeFrequency:"monthly" as const,priority:.6}))]}
