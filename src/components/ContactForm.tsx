"use client";
import { useState } from "react";
export function ContactForm(){const [state,setState]=useState<"idle"|"loading"|"success"|"error">("idle");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setState("loading");const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});setState(r.ok?"success":"error");if(r.ok)e.currentTarget.reset()}return <form onSubmit={submit} className="grid gap-5">{[["name","Full name","text"],["email","Email address","email"],["subject","Subject","text"]].map(([name,label,type])=><label key={name} className="grid gap-2 text-sm font-bold text-[#0b1f3a]">{label}<input required name={name} type={type} className="focus-ring rounded-xl border border-[#d4dde5] px-4 py-3 font-normal"/></label>)}<label className="grid gap-2 text-sm font-bold text-[#0b1f3a]">Message<textarea required minLength={10} rows={6} name="message" className="focus-ring resize-y rounded-xl border border-[#d4dde5] px-4 py-3 font-normal"/></label><button disabled={state==="loading"} className="rounded-xl bg-[#147a4b] px-5 py-3.5 font-bold text-white disabled:opacity-60">{state==="loading"?"Sending…":"Send message"}</button>{state==="success"&&<p role="status" className="text-sm font-semibold text-[#147a4b]">Thank you. Your message has been received.</p>}{state==="error"&&<p role="alert" className="text-sm font-semibold text-red-700">Unable to send. Please review the fields and try again.</p>}</form>}
